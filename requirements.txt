# Driver Behaviour Monitoring - Python dependencies
# Python 3.10.  Install with:  pip install -r requirements.txt
#
# Note: native-Windows TensorFlow >= 2.11 is CPU-only. For GPU training use WSL2
# or a Linux machine with the matching CUDA/cuDNN build of TensorFlow.

tensorflow==2.21.0
numpy==2.2.6
scikit-learn==1.7.2      # classification report + confusion matrix
matplotlib==3.10.8       # confusion-matrix figure
opencv-python==4.13.0.92 # webcam capture / rendering (realtime_detect.py)
firebase-admin==7.4.0    # push alerts to Firebase (realtime_detect.py)
